version: '2.1'

services:

  librespot:
  
    build: .
    container_name: librespot
    image: librespot-java:latest
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./user-data:/config
    devices:
      - /dev/snd:/dev/snd
